export async function createPost({ input } = {}) {
  // validate input as post model
  // write to db
  // transform data model to response model

  // return input as a placeholder
  return input;
}
